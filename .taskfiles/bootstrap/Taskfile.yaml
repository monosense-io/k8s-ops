---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  talos:
    desc: Bootstrap Talos cluster (cluster-aware)
    cmds:
      - echo "Bootstrap Talos cluster for {{.CLUSTER}}"
      - echo "TODO - Implement in Story 1.3: Bootstrap Infra Cluster"

  apps:
    desc: Bootstrap Kubernetes applications via Helmfile (cluster-aware)
    cmds:
      - echo "Bootstrap apps for {{.CLUSTER}}"
      - echo "TODO - Implement in Story 1.2: Create Helmfile Bootstrap Configuration"
